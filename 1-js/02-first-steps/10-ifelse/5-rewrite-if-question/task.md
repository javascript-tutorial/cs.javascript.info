importance: 5

---

# Přepište „if“ na „?“

Přepište tento příkaz `if` pomocí podmíněného operátoru `'?'`:

```js
let výsledek;

if (a + b < 4) {
  výsledek = 'Málo';
} else {
  výsledek = 'Moc';
}
```
